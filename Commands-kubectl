kubectl get componentstatuses
kubectl get node
kubectl get ns
kubectl get pod -n rbl -o wide

kubectl run pod1 --image=docker.in/nginx 	# This to create a pod

kubectl get pod -n rbl -o wide
kubectl logs pod1 -n rbl
kubectl exec -it pod1 -n rbl -- bash
kubectl describe pod pod1 -n rbl
kubectl get sa -n network
kubectl delete pod pod1 -n rbl
kubectl delete pod --all -n rbl
kubectl delete ns rbl
kubectl  logs pod1  -n rbl                          # to see logs of pod
kubectl  exec   -it  pod1  -n rbl   -- bash         # to enter into pod 
kubectl describe  pod  pod1  -n krnetwork           # to see the indepth information of a pod, it is like docker inspect
kubectl get sa -n krnetwork                         # it see the api service account, this service account automaticaly created in each project 
kubectl run MySQL --image=docker.io/MySQL:5.6 --env MYSQL_PASSWORD=redhat -n rbl       #  -env use to pass enviremnt variable into command




kubectl run demo --image=docker.io/nginx --dry-run -o yaml                             # This is to get outpot in a yaml from commandline mode
kubectl create -f pod.yaml -n rbl         # Create POD using dec. mode..

kubectl api-resources
kubectl explain pod
kubectl explain pod.metadata
kubectl explain pod.spec
kubectl explain pod.spec.containers
kubectl explain pod.spec.containers --recursive=true      # to see the how many keywords can come under container
kubectl get rc
kubectl create ns trading
kubectl create -f replication.yaml -n trading  or kubectl apply -f replication.yaml -n trading         # For RC yaml you can refer k8s website
kubectl get pod -n trading -o wide
kubectl scale --replicas=5 rc test-rc -n trading                                                        # To scale up the number of POD as per our need
kubectl edit rc test-rc -n trading

kubectl get rc test-rc -o yaml > rc.yaml   # It is to retrive if RC manifiest deleted accedently...


kubectl run   pod1 --image=nginx  -l   tier=backend,zone=mumbai  # put lable on pod
kubectl get pod
kubectl get pod  --show-labels
kubectl run   pod2  --image=nginx  -l   tier=backend,zone=delhi
kubectl get pod  --show-labels
kubectl get pod -l  zone=mumbai
kubectl get pod
kubectl get pod -l  zone=mumbai
kubectl get pod -l  zone=delhi
kubectl get pod
kubectl label  pod  pod1  region=india
kubectl label  pod  pod2  region=us
kubectl get pod --show-labels
kubectl delete  pod  -l region=india -A
kubectl get pod
kubectl get pod --show-labels
kubectl label  pod  pod2  region=us  --overwrite
kubectl label  pod  pod2  region=india   --overwrite
kubectl get pod --show-labels

kubectlget pod --selector-zone=india
kubectlget pod --selector-zone!=india



1. Match Pods with a specific label
kubectl get pods --selector app=nginx
Returns only pods with label app=nginx.

2. Match multiple labels
kubectl get pods -l app=nginx,env=prod
Labels must all match (app=nginx AND env=prod).

3. Match using in
kubectl get pods -l 'app in (nginx,apache)'
Pods with app=nginx OR app=apache.

4. Match using notin
kubectl get pods -l 'env notin (dev,test)'
Exclude pods with env=dev or env=test.

5. Match label existence
kubectl get pods -l 'env'
All pods that have the env label (value can be anything).

6. Match where label does not exist
kubectl get pods -l '!env'
Pods without an env label.

7. Get nodes with a specific label
kubectl get nodes -l disktype=ssd

8. Get all services with a label
kubectl get svc -l tier=backend

9. Combine with watch
kubectl get pods -l app=nginx -w




















🔍 Cluster and Context Management
Command	Description
kubectl config get-contexts	List all available contexts.
kubectl config use-context <context-name>	Switch to a specific context.
kubectl config current-context	Show the current context.
kubectl cluster-info	Show cluster master and service info.
kubectl get componentstatuses	Get status of cluster components (deprecated in newer versions).

🧠 Nodes and System Health
Command	Description
kubectl get nodes	List all nodes in the cluster.
kubectl describe node <node-name>	Get detailed info about a specific node.
kubectl cordon <node>	Mark a node as unschedulable.
kubectl drain <node> --ignore-daemonsets	Prepare a node for maintenance.
kubectl uncordon <node>	Make a node schedulable again.

📦 Pods and Workloads
Command	Description
kubectl get pods	List all pods in the current namespace.
kubectl get pods -A	List pods in all namespaces.
kubectl describe pod <pod-name>	Detailed info about a pod.
kubectl logs <pod-name>	Show logs of a pod.
kubectl logs <pod-name> -c <container>	Logs of a specific container.
kubectl exec -it <pod> -- /bin/bash	Execute command in a pod (interactive shell).
kubectl delete pod <pod-name>	Delete a pod.
kubectl port-forward <pod-name> 8080:80	Forward local port to pod port.

🎯 Deployments, ReplicaSets, StatefulSets
Command	Description
kubectl get deployments	List deployments.
kubectl describe deployment <name>	Describe deployment.
kubectl rollout status deployment <name>	Monitor rollout status.
kubectl rollout restart deployment <name>	Restart deployment pods.
kubectl scale deployment <name> --replicas=3	Scale a deployment.
kubectl delete deployment <name>	Delete a deployment.

⚙️ Services, Endpoints, Ingress
Command	Description
kubectl get svc	List services.
kubectl describe svc <name>	Get detailed service info.
kubectl get endpoints	Show service endpoints.
kubectl get ingress	List Ingress resources.
kubectl describe ingress <name>	Describe an Ingress.

📁 Namespaces and Contexts
Command	Description
kubectl get ns	List all namespaces.
kubectl create ns <name>	Create a new namespace.
kubectl delete ns <name>	Delete a namespace.
kubectl config set-context --current --namespace=<ns>	Set default namespace for current context.

📜 YAML and Resource Management
Command	Description
kubectl apply -f <file>.yaml	Create/update resources from YAML.
kubectl create -f <file>.yaml	Create resource from YAML.
kubectl delete -f <file>.yaml	Delete resource from YAML.
kubectl edit <resource>/<name>	Edit resource live in your editor.
kubectl get <resource> -o yaml	Output resource as YAML.

🔧 Troubleshooting and Debugging
Command	Description
kubectl describe <resource> <name>	Detailed info on any resource.
kubectl logs <pod>	Show logs.
kubectl get events --sort-by='.metadata.creationTimestamp'	Show events chronologically.
kubectl top pod	Show CPU/memory usage of pods (metrics-server required).

🛠️ Custom Commands and Utilities
Command	Description
kubectl explain <resource>	Show schema of a resource (e.g., kubectl explain pod.spec.containers).
kubectl api-resources	List all resource types.
kubectl version	Show client and server version info.
